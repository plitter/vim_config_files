/TEMPORARY README WILL BE BETTER LATER!!!!

First you have to make a symbolic link to the vimrc that comes
with the repository. It can be done like this:
	ln -s /absolute-path/to/repository/vimrc $(HOME)/.vimrc
